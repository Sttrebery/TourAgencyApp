<Page x:Class="TourAgencyApp.Views.Employee.Pages.CountriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TourAgencyApp.Views.Employee.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CountriesPage">

    <ScrollViewer Background="AliceBlue" VerticalScrollBarVisibility="Auto">
        <TabControl>
            <TabItem Header="Добавление страны">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    
                    
                    <StackPanel>
                        <Button Margin="10" Width="100" HorizontalAlignment="Left" Command="{Binding UpdateListCommmand}">Обновить</Button>
                        <ScrollViewer Grid.Row="0" Margin="5" VerticalScrollBarVisibility="Auto" >
                            <ListView x:Name="CountriesListView" ItemsSource="{Binding Countries}" HorizontalContentAlignment="Center" FontSize="14">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="ID" Width="100">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Path=ID}"></TextBlock>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Название" Width="350">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Path=Name}"></TextBlock>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </ScrollViewer>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="10">
                        <Label FontWeight="DemiBold" FontSize="18">Добавление страны</Label>
                        <Border BorderBrush="Black" BorderThickness="2"></Border>
                        <TextBlock Margin="5" FontSize="16">Введите название страны:</TextBlock>
                        <TextBox Margin="5" FontSize="16" Text="{Binding CountryToAdd}"></TextBox>
                        <Button Margin="5" Width="150" Command="{Binding AddCountryCommmand}">Добавить</Button>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Редактирование страны">

            </TabItem>
        </TabControl>
    </ScrollViewer>
</Page>
