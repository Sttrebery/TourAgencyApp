<Page x:Class="TourAgencyApp.Views.Client.Pages.Popularity"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TourAgencyApp.Views.Client.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Popularity">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Dictionaries/PagesDictionary.xaml"/>
                <ResourceDictionary Source="/Dictionaries/MainWindowDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <ScrollViewer Background="#c2dbfc" VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.RowSpan="2" Style="{StaticResource BorderStyle1}">
                <StackPanel Margin="10">
                    <Label Style="{StaticResource LabelStyle2}">Популярные туры</Label>
                    <ScrollViewer Margin="10" VerticalScrollBarVisibility="Auto">
                        <ListView ItemsSource="{Binding TopTours}" >
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Название" Width="100">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" TextWrapping="Wrap"></TextBlock>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Описание" Width="200">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"></TextBlock>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Стоимость" Width="100">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Cost}"></TextBlock>
                                                    <TextBlock Text=" руб."></TextBlock>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Даты проведения" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding StartDate, StringFormat=dd.MM.yyyy}" TextWrapping="Wrap"></TextBlock>
                                                    <TextBlock Text="-" TextWrapping="Wrap"></TextBlock>
                                                    <TextBlock Text="{Binding EndDate, StringFormat=dd.MM.yyyy}" TextWrapping="Wrap"></TextBlock>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                    <!--- Кнопка подробнее -->
                                    <GridViewColumn Header="Детали" Width="100">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Command="{Binding DataContext.ShowDetailCommand, 
                             RelativeSource={RelativeSource AncestorType=ListView}}"
                             CommandParameter="{Binding}">
                                                    Подробнее
                                                </Button>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource BorderStyle1}">
                <StackPanel Margin="3">
                    <WrapPanel Margin="0,10,0,10">
                        <Label Style="{StaticResource LabelStyle2}">Самая популярная страна:</Label>
                        <TextBlock Margin="20,0,20,0" FontSize="20" Padding="5" TextDecorations="Underline" Text="{Binding TopCountryName}"></TextBlock>
                    </WrapPanel>

                    <WrapPanel Margin="0,10,0,10">
                        <Label Style="{StaticResource LabelStyle2}" >Количество туров, посещающих эту страну:</Label>
                        <TextBlock FontSize="20" Padding="5" Margin="20,0,20,0" FontWeight="Bold" Text="{Binding TopCountryCount}"></TextBlock>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>

</Page>
